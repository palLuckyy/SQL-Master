{
  "name": "sql-master",
  "version": "1.0.0",
  "description": "SQL Master — Full-stack learning platform",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev:backend":  "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "setup": "npm run install:all && cd backend && cp .env.example .env; cd ../frontend && cp .env.example .env",
    "build": "cd frontend && npm run build"
  }
}
